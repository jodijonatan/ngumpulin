# File: ngumpulin/public/.htaccess (Bisa Dihapus jika menggunakan Root .htaccess)

<IfModule mod_rewrite.c>
    # Jika file index.php ada di public/, tidak ada rule yang diperlukan di sini.
    # Aturan di root sudah mengurusnya.
</IfModule>